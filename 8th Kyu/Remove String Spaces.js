// Simple, remove the spaces from the string, then return the resultant string.

function noSpace(x){
    return x.split(' ').join('')
  }
  
  //PREP
  //PARAMETERS
  //Enter a string
  //RETURNS
  //Return the string with no spaces in it
  //EXAMPLES
  //"a b cde" --> "abcde"
  //PSEUDO
  //Split the string with a blank space, and then rejoin it with no space