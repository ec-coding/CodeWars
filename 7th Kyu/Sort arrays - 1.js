// Just a simple sorting usage. Create a function that returns the elements of the input-array / list sorted in lexicographical order.

// input: names - unsorted array
// output: sorted array
sortme = function( names ){
    return names.sort()
  }
  
  // PREP
  // PARAMETERS
  // Enter an array
  // RETURNS
  // Return the array with the elements sorted in lexicographical order
  // EXAMPLES
  // ["five", "six", "eight"] -> ["eight", "five", "six"]
  // PSEUDO
  // Use the .sort() method